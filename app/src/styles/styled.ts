import { Container, StatsContainer } from "./container";
import { Label, StatsTitle } from "./label";
import { Button, HoverButton } from "./button";
import { StatsList, StatsListElement } from "./list";

export const Styled = {
  Container,
  StatsContainer,
  Label,
  StatsTitle,
  Button,
  HoverButton,
  StatsList,
  StatsListElement
}