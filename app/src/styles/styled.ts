import { Container, StatsContainer, ContainerVertical } from "./container";
import { Label, StatsTitle } from "./label";
import { Button, HoverButton } from "./button";
import { StatsList, StatsListElement } from "./list";

export const Styled = {
  Container,
  ContainerVertical,
  StatsContainer,
  Label,
  StatsTitle,
  Button,
  HoverButton,
  StatsList,
  StatsListElement
}